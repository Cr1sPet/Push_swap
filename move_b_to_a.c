#include "push_swap.h"

int	find_elem_to_push (stack_a, stack_b)
{
	
	return (0);
}

void move_b_to_a(t_list **stack_a, t_list **stack_b)
{
	int elem_numb;

	while (*stack_b)
	{
		elem_numb = find_elem_to_push (stack_a, stack_b);
	}
}